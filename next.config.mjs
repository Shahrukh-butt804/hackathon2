// /** @type {import('next').NextConfig} */
// const nextConfig = {
    
// };

// export default nextConfig;
/** @type {import('next').NextConfig} */
const nextConfig = {
    eslint: {
      // This setting allows production builds to complete even with ESLint errors
      ignoreDuringBuilds: true,
      // reactStrictMode: false,
    },
  };
  
  export default nextConfig;
  